meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    //   "email": "test1@test.net",
    "email": "admin@global-academy.com",
  //   "email": "saas@test.net",
    "password": "something123"
  }
}

script:post-response {
  const accessToken = res.getHeaders()['set-cookie']
  console.log("token", accessToken[0])
  bru.setEnvVar('token', accessToken)
}

settings {
  encodeUrl: true
  timeout: 0
}

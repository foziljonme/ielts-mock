meta {
  name: Change Password
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/auth/change-password
  body: json
  auth: inherit
}

body:json {
  {
    "currentPassword": "demo",
    "newPassword": "something123"
  }
}

script:post-response {
  const accessToken = res.getBody().accessToken
  bru.setEnvVar('token', accessToken)
}

settings {
  encodeUrl: true
  timeout: 0
}

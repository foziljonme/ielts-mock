meta {
  name: Login
  type: http
  seq: 6
}

post {
  url: {{BASE_URL}}/auth/candidate/login
  body: json
  auth: inherit
}

body:json {
  {
    
  }
}

script:post-response {
  const accessToken = res.getBody().accessToken
  bru.setEnvVar('token', accessToken)
}

settings {
  encodeUrl: true
  timeout: 0
}
